{
  "description": "compact-adviser: judges a completed Codex turn and, when the session is at a good boundary, prints one hint to run /compact",
  "hooks": {
    "Stop": [
      {
        "description": "Judge the completed checkpoint and hint when it qualifies",
        "hooks": [
          {
            "type": "command",
            "command": "sh \"${PLUGIN_ROOT}/hooks/run.sh\" \"${PLUGIN_ROOT}/src/hook.ts\"",
            "timeout": 15,
            "statusMessage": "compact-adviser: checking this checkpoint"
          }
        ]
      }
    ],
    "PreCompact": [
      {
        "description": "Reset this session's cooldown counters around a compaction",
        "hooks": [
          {
            "type": "command",
            "command": "sh \"${PLUGIN_ROOT}/hooks/run.sh\" \"${PLUGIN_ROOT}/src/hook.ts\"",
            "timeout": 10
          }
        ]
      }
    ],
    "PostCompact": [
      {
        "description": "Reset this session's cooldown counters around a compaction",
        "hooks": [
          {
            "type": "command",
            "command": "sh \"${PLUGIN_ROOT}/hooks/run.sh\" \"${PLUGIN_ROOT}/src/hook.ts\"",
            "timeout": 10
          }
        ]
      }
    ],
    "SessionStart": [
      {
        "description": "Start a session's counters, or reset them when it resumes from a compaction",
        "hooks": [
          {
            "type": "command",
            "command": "sh \"${PLUGIN_ROOT}/hooks/run.sh\" \"${PLUGIN_ROOT}/src/hook.ts\"",
            "timeout": 10
          }
        ]
      }
    ]
  }
}
